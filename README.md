# MyLibr - Личная библиотека на Django

Веб-приложение для управления личной библиотекой книг.

## 📋 Функционал

- 🔐 **Регистрация и аутентификация пользователей** через email (django-allauth)
- 📖 Просмотр всех книг с пагинацией (6 книг на странице)
- 🔍 Подробная информация о каждой книге
- 📊 Статистика библиотеки
- ➕ Добавление новых книг (только для авторизованных пользователей)
- ✏️ Редактирование существующих книг (только для авторизованных пользователей)
- 🗑️ Удаление книг (только для авторизованных пользователей)
- 👤 Добавление новых авторов и жанров прямо в форме
- 🖼️ Загрузка обложек книг
- 📧 Подтверждение email при регистрации
- 🔑 Восстановление пароля

# 🚀 Инструкция по установке и запуску
## Варианты запуска Docker
### 1. Установите Docker (если нет)

Windows/Mac:

    Скачайте Docker Desktop

    Установите как обычную программу

    Запустите Docker Desktop (дождитесь зелёной иконки в трее)

Linux:
```bash

sudo apt update
sudo apt install docker.io docker-compose
sudo systemctl start docker
````
### 2. Варианты установвки:
#### Вариант A: Автоматический скрипт
```bash
git clone https://github.com/Calton-GT/MyLIbr.git
cd MyLibr
chmod +x start-docker.sh    # Только для Linux/Mac/Git Bash
./start-docker.sh   
```

#### Вариант B: Пошагово вручную
```bash
# 1. Скачать проект
git clone https://github.com/Calton-GT/MyLIbr.git

# 2. Перейти в папку
cd MyLibr

# 3. Собрать Docker образы
docker-compose build

# 4. Запустить контейнеры
docker-compose up -d

# 5. Создать администратора
docker-compose exec web python manage.py createsuperuser --username admin --email admin@example.com --noinput
docker-compose exec web python manage.py shell -c "from django.contrib.auth import get_user_model; User = get_user_model(); u = User.objects.get(username='admin'); u.set_password('admin123'); u.save()"

# Готово! Открывайте http://localhost:8000
```
### 3. Откройте в браузере

После запуска откройте браузер и перейдите по адресам:

    🌐 Основной сайт: http://localhost:8000

    🔧 Админ-панель: http://localhost:8000/admin

    👤 Логин: admin

    🔑 Пароль: admin123

## ⚙️ Управление проектом
### Основные команды:
```bash

# Проверить статус
docker-compose ps

# Просмотреть логи
docker-compose logs -f

# Остановить проект
docker-compose down

# Перезапустить
docker-compose restart

# Войти внутрь контейнера (терминал)
docker-compose exec web bash

# Обновить статические файлы
docker-compose exec web python manage.py collectstatic --noinput
```
### Полная остановка и очистка:
```bash

# Остановить и удалить всё
docker-compose down && docker system prune -a -f

# Или используйте готовый скрипт
./stop-docker.sh
```

# 🐛 Решение проблем
#### Проблема 1: "Порт 8000 уже используется"
```bash

# Измените порт в docker-compose.yml на:
ports:
  - "8080:8000"
# Теперь сайт будет на http://localhost:8080
````
#### Проблема 2: "Docker не запускается"

    Перезапустите Docker Desktop

    Убедитесь в трее зелёная иконка

    Проверьте: docker --version

#### Проблема 3: "Ошибка при сборке"
```bash

# Полная очистка и пересборка
docker-compose down
docker system prune -a -f
docker-compose build --no-cache
docker-compose up -d
````
#### Проблема 4: "Не могу войти в админку"
```bash

# Сбросить пароль администратора
docker-compose exec web python manage.py changepassword admin
# Введите новый пароль
````
## Варианты запуска без использования Docker 
### Предварительные требования

- **Python 3.8 или выше** (рекомендуется Python 3.9+)
- **pip** (менеджер пакетов Python, обычно устанавливается вместе с Python)
- **Git** (опционально, если нужно клонировать репозиторий)

Для проверки установки откройте терминал/командную строку и выполните:
```bash
python --version
# или
python3 --version

pip --version
# или
pip3 --version
```

### Пошаговая установка

#### 1. Получение проекта

Если проект уже скачан:
- Перейдите в папку проекта `personal_library`

Если нужно клонировать из репозитория:
```bash
git clone https://github.com/Calton-GT/MyLIbr.git
cd MyLIbr/personal_library
```

#### 2. Создание виртуального окружения (рекомендуется)

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

После активации в начале строки терминала появится `(venv)`.

#### 3. Установка зависимостей

Убедитесь, что виртуальное окружение активировано, затем установите необходимые пакеты:

```bash
pip install -r requirements
```

Или, если файл называется `requirements.txt`:
```bash
pip install -r requirements.txt
```

**Устанавливаемые пакеты:**
- Django>=4.2
- Pillow>=10.0.0 (для работы с изображениями обложек)
- django-allauth>=0.57.0 (для регистрации и аутентификации пользователей)

#### 4. Выполнение миграций базы данных

Миграции создадут необходимые таблицы в базе данных SQLite:

```bash
python manage.py migrate
```

**Windows:**
```bash
py manage.py migrate
```

**Linux/Mac:**
```bash
python3 manage.py migrate
```

Вы должны увидеть сообщения о применении миграций.

#### 5. Настройка сайта (Site) для django-allauth

django-allauth требует наличие объекта Site в базе данных. Выполните:

**Windows:**
```bash
py manage.py shell
```

**Linux/Mac:**
```bash
python3 manage.py shell
```

Затем в интерактивной оболочке Python:
```python
from django.contrib.sites.models import Site
site = Site.objects.get(id=1)
site.domain = '127.0.0.1:8000'
site.name = 'MyLibr'
site.save()
exit()
```

**Альтернативный способ:** Используйте админ-панель Django после создания суперпользователя:
```bash
py manage.py createsuperuser
```
Затем перейдите на http://127.0.0.1:8000/admin/sites/site/ и настройте сайт.

#### 6. (Опционально) Создание тестовых данных

Проект автоматически создаст тестовые данные при первом запуске, если в базе меньше 5 книг. Но вы также можете создать их вручную:

**Windows:**
```bash
py manage.py shell -c "from library.views import create_sample_data; create_sample_data()"
```

**Linux/Mac:**
```bash
python3 manage.py shell -c "from library.views import create_sample_data; create_sample_data()"
```

Это создаст 20 тестовых книг с авторами и жанрами.

#### 7. Запуск сервера разработки

**Windows:**
```bash
py manage.py runserver
```

**Linux/Mac:**
```bash
python3 manage.py runserver
```

Или используйте команду:
```bash
python manage.py runserver
```

После успешного запуска вы увидите сообщение:
```
Starting development server at http://127.0.0.1:8000/
Quit the server with CTRL-BREAK.
```

#### 8. Открытие приложения в браузере

Откройте браузер и перейдите по адресу:
```
http://127.0.0.1:8000/
```

или

```
http://localhost:8000/
```

### Использование приложения

#### Регистрация и аутентификация

- **Регистрация** (`/accounts/signup/`) - регистрация нового пользователя через email
- **Вход** (`/accounts/login/`) - вход в систему по email и паролю
- **Выход** (`/accounts/logout/`) - выход из системы
- **Восстановление пароля** (`/accounts/password/reset/`) - восстановление забытого пароля

**Важно:** После регистрации проверьте консоль (терминал, где запущен сервер) - там будет ссылка для подтверждения email. Перейдите по ссылке, чтобы завершить регистрацию.

#### Работа с книгами

- **Главная страница** (`/`) - список всех книг с пагинацией (доступна всем)
- **Детали книги** (`/book/<id>/`) - подробная информация о книге (доступна всем)
- **Статистика** (`/statistics/`) - общая статистика библиотеки (доступна всем)
- **Добавить книгу** (`/add-book/`) - форма для добавления новой книги ⚠️ **Требуется авторизация**
- **Редактировать книгу** (`/edit-book/<id>/`) - редактирование существующей книги ⚠️ **Требуется авторизация**
- **Удалить книгу** (`/delete-book/<id>/`) - удаление книги с подтверждением ⚠️ **Требуется авторизация**

## 🔧 Возможные проблемы и решения

### Проблема: "python: command not found"
**Решение:**
- Windows: используйте `py` вместо `python`
- Linux/Mac: используйте `python3` вместо `python`
- Убедитесь, что Python установлен и добавлен в PATH

### Проблема: "pip: command not found"
**Решение:**
- Установите pip: `python -m ensurepip --upgrade`
- Или используйте `pip3` на Linux/Mac

### Проблема: "ModuleNotFoundError: No module named 'django'"
**Решение:**
- Убедитесь, что виртуальное окружение активировано
- Переустановите зависимости: `pip install -r requirements`

### Проблема: "Port 8000 is already in use"
**Решение:**
- Завершите процесс, использующий порт 8000
- Или запустите сервер на другом порту: `python manage.py runserver 8001`

### Проблема: "Database locked" или ошибки базы данных
**Решение:**
- Убедитесь, что база данных `db.sqlite3` не открыта в другой программе
- Удалите файл `db.sqlite3` и выполните миграции заново:
  ```bash
  python manage.py migrate
  ```

### Проблема: "Site matching query does not exist" или ошибки с allauth
**Решение:**
- Выполните шаг 5 из инструкции по установке (настройка Site)
- Или создайте Site через админ-панель Django

### Проблема: Письмо с подтверждением email не приходит
**Решение:**
- В режиме разработки письма выводятся в консоль (терминал, где запущен сервер)
- Проверьте консоль на наличие ссылки для подтверждения email
- Для продакшена настройте SMTP-сервер в `settings.py`

## 📁 Структура проекта

```
MyLibr/
├── library/                    # Основное приложение
│   ├── models.py              # Модели данных (Book, Author, Genre)
│   ├── views.py               # Представления (логика страниц)
│   ├── forms.py               # Формы для работы с данными
│   ├── urls.py                # URL-маршруты
│   └── templates/             # HTML-шаблоны
│       ├── library/           # Шаблоны библиотеки
│       └── account/           # Шаблоны для allauth (login, signup, etc.)
├── personal_library/           # Настройки проекта Django
│   ├── settings.py            # Основные настройки (включая allauth)
│   └── urls.py                # Главный файл маршрутов
├── media/                     # Загружаемые файлы (обложки книг)
├── static/                    # Статические файлы (CSS, JS)
├── db.sqlite3                 # База данных SQLite
├── manage.py                  # Управляющий скрипт Django
├── requirements.txt           # Зависимости проекта
├── Dockerfile                    # Конфигурация Docker
├── docker-compose.yml            # Настройка сервисов
├── start-docker.sh              # Автозапуск (копируйте и запускайте!)
└── stop-docker.sh               # Автоостановка
```

## 🔐 Система аутентификации

Проект использует **django-allauth** для регистрации и аутентификации пользователей.

### Особенности

- **Вход по email** (без username)
- **Обязательное подтверждение email** при регистрации
- **Защита функционала** - добавление, редактирование и удаление книг доступны только авторизованным пользователям
- **Восстановление пароля** через email

### Настройки email

В режиме разработки все письма выводятся в консоль (терминал, где запущен сервер). Это настраивается в `settings.py`:

```python
EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'
```

Для продакшена замените на настройки реального SMTP-сервера.

### Отключение подтверждения email (только для разработки)

Если хотите отключить обязательное подтверждение email в режиме разработки, измените в `settings.py`:

```python
ACCOUNT_EMAIL_VERIFICATION = 'none'  # Только для разработки!
```

⚠️ **Внимание:** Не рекомендуется отключать подтверждение email в продакшене.

## 🛠️ Дополнительная информация

- **База данных:** SQLite (файл `db.sqlite3` в корне проекта)
- **Язык интерфейса:** Русский
- **Версия Django:** 5.2.8
- **Python:** 3.8+
- **Аутентификация:** django-allauth>=0.57.0

## ⚠️ Важные замечания

1. Этот проект использует настройки для разработки (`DEBUG = True`). Для продакшена необходима дополнительная настройка безопасности.

2. База данных SQLite хранится в файле `db.sqlite3`. Не удаляйте этот файл, если не хотите потерять данные.

3. Виртуальное окружение (`venv/`) обычно не включается в репозиторий. Создавайте его самостоятельно при установке проекта.

## 📝 Лицензия

Этот проект создан для личного использования.

## 🤝 Поддержка

При возникновении проблем:
1. Убедитесь, что все шаги установки выполнены правильно
2. Проверьте версию Python (должна быть 3.8 или выше)
3. Убедитесь, что виртуальное окружение активировано
4. Проверьте, что все зависимости установлены

---

**Удачной работы с библиотекой! 📚**
